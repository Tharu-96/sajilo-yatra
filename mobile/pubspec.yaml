name: sajiloyatra
description: Sajilo Yatra Cinematic Transit
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  go_router: ^12.1.1
  http: ^1.1.0
  google_fonts: ^6.1.0
  flutter_dotenv: ^5.1.0
  geolocator: ^13.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - .env
